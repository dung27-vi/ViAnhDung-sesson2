#include <stdio.h>

int main() {
    // Khai báo và kh?i t?o hai bi?n s? nguyên
    int so_a = 5;
    int so_b = 10;

    // Tính t?ng c?a hai bi?n và lýu k?t qu?
    int tong = so_a + so_b;

    // In k?t qu? ra màn h?nh (tùy ch?n)
    printf("Tong cua %d va %d la: %d\n", so_a, so_b, tong);

    return 0;
}

